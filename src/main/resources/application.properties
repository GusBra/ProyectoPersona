
## PostgreSQL - PersonaDB
spring.datasource.url=jdbc:postgresql://localhost:5433/PersonaDB
spring.datasource.username=postgres
spring.datasource.password=huevito1
spring.datasource.driver-class-name=org.postgresql.Driver


#drop n create table again, good for testing, comment this in production
spring.jpa.show-sql=true
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update









# App Properties
#twins.app.jwtSecret= bezKoderSecretKey5
#twins.app.jwtExpirationMs= 86400000

## default connection pool
#spring.datasource.hikari.connectionTimeout=20000
#spring.datasource.hikari.maximumPoolSize=5






## MULTIPART (MultipartProperties)
# Enable multipart uploads
#-spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
#-spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
#-spring.servlet.multipart.max-file-size=200MB
# Max Request Size
#-spring.servlet.multipart.max-request-size=215MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
#file.upload-dir=C:/xampp/htdocs/img/upgrade
#file.url-dir=http://localhost/img/upgrade